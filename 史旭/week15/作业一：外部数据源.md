# 1. 方案-天池三轮车

## 搜索引擎

```markdown
from baidusearch.baidusearch import search # 百度、DuckDuckGo 和 Google 三大搜索引擎获取搜索结果 搜索引擎
from duckduckgo_search import DDGS # DuckDuckGo 搜索引擎
from googlesearch import search # Google 搜索引擎

**使用 百度、DuckDuckGo 和 Google 三大搜索引擎获取搜索结果，用于网络信息采集。**
```

## 数据源

**主要使用的是 Elasticsearch 文档数据库（具备向量检索功能），对解析的文档信息进行存储.
通过对问题进行bge编码，之后在Elasticsearch中检索相关信息，通过Agent和MCP生成报告（RAG流程，加入了DeepResearch 更复杂的RAG）**

# 2. 方案-好想成为人类

## 搜索引擎

```markdown
import akshare as ak # AKShare 专注于中文金融数据的开源库
from sogou_search import sogou_search # 搜狐 搜索引擎
from duckduckgo_search import DDGS # DuckDuckGo 搜索引擎

https: // www.bing.com / search?q = {keywords} & count = {max_results} # Bing 搜索引擎
https: // www.sensetime.com / cn / about - index # 1 # 网站url

**从 DuckDuckGo、搜狐、AKShare（专注于中文金融数据的开源库）以及相关网站API（Bing）、URL等获取数据信息。**
```

## 数据源

**未使用外部数据源，主要是从搜索引擎中读取数据，然后保存到本地文件夹下（如txt、md等文件），之后使用Agent智能体构建RAG
（主要分为四部分：研报生成的决策 -》 信息搜索 -》 章节生成 -》 研报完成）**

# 3. 方案-队伍名字不能为空

## 搜索引擎

```markdown
'https://data.stats.gov.cn/easyquery.htm'  # 国家数据（国家统计局官方数据库）前端入口

'https://data.stats.gov.cn/easyquery.htm?m=QueryData&dbcode={time}&rowcode=zb&colcode=sj&wds=%5B%5D&dfwds={key}&k1={timestamp}'  #
国家数据 API
'http://www.pbc.gov.cn'  # 中国人民银行官网

**仅通过一些数据网站的API或者URL，直接发送http或https请求获取数据信息**
```

## 数据源

**1. 从网络上下载相关pdf等数据文件，然后将其保存到本地文件夹下**
**2. 通过网站API获取数据，将其保存到milvus向量数据库中**
**3. 通过解析本地文档以及在向量数据库中检索数据，构建上下文信息，最终搭建Agent智能体（RAG流程）**
