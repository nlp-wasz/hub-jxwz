# 整体流程大体返程两个阶段

## 查询数据信息以及生成对应的n条感兴趣的查询

**输入用户历史交互的商品标题序列等信息，之后使用微调后的生成式语言模型（如 GPT-2），生成多条代表用户兴趣的查询**

## 检索

**首先使用上一步生成的多条查询作为输入数据，使用传统的搜索引擎（如 BM25） 检索 top-k个商品信息，
对每条查询检索相关商品信息，按照计算的得分进行排序，然后将所有查询对应的结果进行合并，然后去除重复商品信息，得到最终的结果**
