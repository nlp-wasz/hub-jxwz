DeepWalk 是一种图表示学习（节点嵌入）算法，它通过在图结构上进行随机游走生成节点序列4。这些序列模拟了自然语言中的句子结构。

与 word2vec 
DeepWalk 直接借鉴了 word2vec 的技术框架（尤其是 Skip-gram 模型）：

将随机游走产生的节点序列视为“句子”；

将序列中的节点视为“词语”；

使用 word2vec 的 Skip-gram 模型学习节点的向量表示（节点嵌入）