# 作业1
>AutoDL id：a52f0ec2-648e-42e7-ad1d-f2fb0885ce5f
# 作业2
> 本地开发三种方式的意图识别方案：简单prompt方案、基于少样本+强制工具的方案、基于dify发布api进行意图识别的方案
## 简单prompt方案
> plan_a_prompt.py
## 基于少样本+强制工具的方案
> plan_b_prompt&tool.py
## 基于dify发布api进行意图识别的方案
> 意图识别器.yml
# 作业3
> docker部署三种意图识别方法
> Dockerfile、intent-cls-api/main.py、intent-cls-api/schema.py、intent-cls-api/tool.py、intent-cls-api/.env(该文件未上传)、requirements.txt
## build
```cmd
docker build --progress=plain --no-cache -t intent-cls-api .
```
## run
```cmd
docker run -d -p 8001:8001  -e DIFY_BASE_URL=http://host.docker.internal/v1  --name intent-svc intent-cls-api
```
## load
```cmd
docker load -i intent-cls-api_latest.tar
```
## run
```cmd
docker run -d -p 8001:8001  -e DIFY_BASE_URL=http://host.docker.internal/v1 --name intent-svc intent-cls-api
```
